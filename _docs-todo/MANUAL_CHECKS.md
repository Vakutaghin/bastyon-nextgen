# Ручные проверки на живых сборках

Что код и тесты уже покрывают, но что проверяется только вживую: нужен аккаунт с балансом,
сборка под платформу или второй клиент. Собрано из аудита 2026-09 при его закрытии
(`git show efe6cf3:_docs-todo/APP_AUDIT_2026-09.md`, пометки `[~ needs-live]`) и работы после.
Проверил — вычеркни строку; нашёл баг — заведи задачу.

## С аккаунтом (веб или десктоп)

- [ ] Вход по мнемонике: поле скрыто, «глаз» работает. Вход по WIF (ключ на `5`/`6`/`2`) пускает в тот же аккаунт, что и мнемоника.
- [ ] Регистрация: отмена поверх живой сессии возвращает к ней; перезагрузка посреди регистрации — сид показан после подтверждения; «Обновить капчу» даёт картинку, а не пустое поле.
- [ ] Регистрация, занятое имя (отзыв тестера с Windows 10: «имя занято» не показывалось, после капчи ошибка без повтора, потом `iplimit`): подсказка «имя занято» появляется под полем, пока имя набирают, до капчи. Если сеть всё же отвергла имя после капчи — модалка открывается снова с причиной словами, повтор с другим именем идёт без капчи и без новых монет; после перезапуска та же модалка и пункт «Завершить регистрацию» в меню аватара. `iplimit` — текст про лимит по IP, а не код.
- [ ] Смена аккаунта A→B: подписки, чёрный список, уведомления, избранное и `/limits` — от B; WS-пуши приходят по B.
- [ ] Публикация: пост появляется без F5; при медленной ноде нет дубля (одна транзакция); правка видео-поста сохраняет видео, видимость и язык; удаление убирает пост из ленты.
- [ ] Комментарии: ответ на ответ виден в ветке; счётчик «Осталось N символов» склоняется.
- [ ] Оценки: отредактированный пост оценивается; свой пост оценить нельзя; 1★ показывается как низкая оценка.
- [ ] Уведомления приходят до первого открытия выпадашки и переживают перезапуск; бейдж гаснет после закрытия; тумблеры типов влияют на список.
- [ ] «Сначала лучшее» с окнами сутки / 3 дня / неделя; обрыв сети на догрузке ленты — посты остаются, под ними ошибка и «Повторить».
- [ ] Кошелёк: перевод обновляет баланс и показывает реальную комиссию; bitcoin-адрес отвергается понятным текстом; история не зависит от ноды, закреплённой в эксплорере.
- [ ] Чёрный список убирает автора из ленты, бустов и рекомендаций и глушит переписку.
- [ ] Профиль: смена имени на «a/b» или со словом «bastyon» отвергается; сохранение «О себе» со старым именем проходит.
- [ ] Настройки → Система: тумблер автозапуска совпадает с системой после выключения автозапуска в настройках ОС.

## Мессенджер

- [ ] **Совместимость с legacy `bastyon-chat` (обязательно, меняли формат комнат — S42):** новый диалог из nextgen виден там как тот же чат, текст читается в обе стороны, кнопка PKOIN на месте.
- [ ] Фото, видео и файл в чате показываются медиа, а не hex-строкой.
- [ ] Голосовое с переключением чата уходит в свой чат; Ctrl+V скриншота в композер поста при свёрнутом виджете не уходит собеседнику; свёрнутое окно не помечает сообщения прочитанными.
- [ ] Обрыв сети при отправке → «Не отправлено» и «Повторить»; отправка тому, кто не заходил в мессенджер, — понятный отказ.

## Десктоп (прод-бандл Tauri: macOS, Windows, Linux)

- [ ] Linux Mint (.deb и AppImage): окно не чёрное. На 0.4.0 было чёрным, в чистой Ubuntu 22.04 под Xvfb та же сборка рисуется — дело в отрисовке WebKitGTK через DMA-BUF (NVIDIA, виртуалки); `main.rs` её выключает. Если чёрное и с новой сборкой — запустить с `WEBKIT_DISABLE_COMPOSITING_MODE=1` и снять вывод терминала.
- [ ] Внешние ссылки и «Поделиться → Twitter» открывают системный браузер; `bastyon://` открывает пост внутри приложения.
- [ ] ffmpeg находится при запуске из Dock; перекодирование 150 МБ доходит до конца; «Отмена» убивает ffmpeg.
- [ ] Удалённая мини-аппа открывается во фрейме; каталожная из «Избранного» открывается после перезапуска; popup закрытой мини-аппы ничего не может; промпт `sign` показывает подписываемую строку; платёж показывает имя приложения.
- [ ] UI сейфа: включение, смена и отключение пароля; «Проверить 12 слов»; модалка «Восстановить по 12 словам / Позже».
- [ ] IPFS: нативные диалоги «Поделиться файлом», окно просмотра `incognito`, первый `ensure` с авторизацией API.
- [ ] Полноэкранный режим плеера; Vimeo-эмбед.

## Tor в десктопе

- [ ] WS открывается, лента грузится после бутстрапа, а не до; health-пинг не висит 120 с; `nc 127.0.0.1 <control-порт>` — отказ.
- [ ] Картинки и iframe режутся CSP в WKWebView и WebView2, `TorImage` грузит по клику; выключение Tor перезагружает окно; `tor_stop` во время загрузки архива её останавливает.
- [ ] Barteron под Tor делает запросы; сайдлоад-мини-аппа на `http://localhost:3000` работает без Tor.

## Оформление (редизайн под Nuxt UI)

Экраны в браузере сняты скриптом `scripts/ui-snapshots.mjs` в обеих темах (план —
`_docs-todo/REDESIGN_NUXT_UI.md`). Здесь то, до чего скрипт не достаёт.

- [ ] Android (Capacitor), слабое устройство: лента прокручивается под полупрозрачной шапкой и нижней панелью с размытием без рывков. Если дёргается — сплошной фон вместо `backdrop-filter` в `app-header/styled.ts` и `bottom-nav.styled.ts`.
- [ ] iOS: Geist с кириллицей, фокус в поле ввода не увеличивает страницу (на телефоне шрифт полей 16px), нижняя панель не заезжает на safe-area.
- [ ] Прод-бандлы Tauri на macOS (WKWebView) и Windows (WebView2): Geist, размытие шапки, тема antd в модалках, выпадашках и тостах, тёмные полосы прокрутки в тёмной теме.
- [ ] Загрузчик видео (только Tauri): список сохранённых видео, прогресс перекодирования, окно сведений, плеер. В браузере открывается только пустая модалка — остальное не видел.
- [ ] Мессенджер с настоящими диалогами: пузыри, реакции, ответы, карточка перевода PKOIN, голосовое, окно перевода PKOIN. Оформление проверено на тестовых данных в сторе.
- [ ] Модалка приветствия после регистрации — в скрипт не попадает.
- [ ] Синий логотип рядом с зелёным акцентом тёмной темы на реальном экране (решение 9 в плане).

## Эксплорер

- [ ] Список пиров с реальной нодой (время в миллисекундах, не «0.0 ms»); «Загрузить ещё» на адресе с несколькими транзакциями в одном блоке.
