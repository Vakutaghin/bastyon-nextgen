{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "default permissions + set-fullscreen (video player) and opener (external links, V40); http scope: any https host + local IPFS gateway (V27) — mini-app URLs are filtered client-side (safe-external-url, V25). Global shortcuts are registered from Rust, so the webview does not need them (N30)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-fullscreen",
    "opener:allow-open-url",
    "dialog:allow-save",
    "dialog:allow-open",
    "dialog:default",
    "fs:allow-write-file",
    "fs:scope-download",
    "deep-link:default",
    "deep-link:allow-register",
    "deep-link:allow-is-registered",
    {
      "identifier": "http:default",
      "allow": [
        {
          "url": "https://**:*/**"
        },
        {
          "url": "http://127.0.0.1:*/**"
        }
      ]
    }
  ]
}
