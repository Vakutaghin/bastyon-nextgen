import styled from 'vue3-styled-components'

/** Разделитель Editor.js (`delimiter`) — в полном виде его не было (S25). */
export const SC_BlockDelimiter = styled.hr`
  margin: 24px auto;
  width: 40%;
  border: 0;
  border-top: 1px solid var(--color-border-light);
`
