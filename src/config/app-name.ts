/**
 * Имя самого приложения (то, что видит пользователь: заголовок окна, вкладка,
 * системные уведомления, шторка на мобильном). Отличается от названия сети:
 * «Bastyon» как проект/протокол остаётся неизменным — его отдаёт мини-аппам
 * `host-context.getProject().name`, на нём же держатся ссылки `bastyon://`
 * и тексты вида «Открыть в Bastyon».
 */
export const APP_NAME = 'Bastyon NextGen'
