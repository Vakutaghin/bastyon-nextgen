/**
 * Общие styled-примитивы, переиспользуемые между фичами.
 *
 * Сюда выносятся стили, которые иначе дублируются 1:1 в нескольких
 * `*.styled.ts` (audit §3.2). Локальные one-off стили остаются рядом со
 * своим компонентом — здесь только то, что действительно общее.
 *
 * Расширять под конкретную поверхность через `styled(SC_Base)` —
 * см. mini-apps-grid.styled.ts.
 */

import styled from 'vue3-styled-components'

/**
 * Заголовок секции-карточки, как у Nuxt UI: 16px/600, яркий текст, без капса
 * (раньше — мелкий капс с трекингом). Используется в блок-эксплорере и
 * каталоге мини-приложений. Отступ нулевой — задаётся на месте использования
 * (через родителя-flex или `styled(SC_SectionTitle)`).
 */
export const SC_SectionTitle = styled.h2`
  margin: 0;
  font-size: 16px;
  font-weight: 600;
  color: var(--ui-text-highlighted);
`
