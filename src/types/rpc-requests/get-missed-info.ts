/**
 * Типы для запроса getmissedinfo API
 *
 * # getmissedinfo - получение пропущенной информации
 *
 * ## TODO: Требуется уточнение параметров
 *
 * Параметры запроса требуют уточнения из оригинального приложения.
 * Пример тела запроса пока не найден.
 *
 * ## Параметры запроса:
 *
 * Параметры зависят от реализации.
 * Требует уточнения из оригинального приложения.
 *
 * **Пример использования:**
 * ```typescript
 * // Получить пропущенную информацию
 * const request: GetMissedInfoRequest = {
 *   method: 'getmissedinfo',
 *   parameters: [
 *     // Параметры требуют уточнения
 *   ],
 *   options: { auth: false }
 * }
 * ```
 *
 * ## В старом приложении:
 *
 * - Вызывается через соответствующий RPC метод
 * - Используется для синхронизации контента после перерыва
 */

import type { BaseRpcRequest } from './common'

/**
 * Параметры запроса getmissedinfo
 *
 * TODO: Требуется уточнение структуры параметров из оригинального приложения.
 * Пример тела запроса пока не найден.
 *
 * Точная структура параметров требует уточнения из оригинального приложения.
 * Обычно включает информацию о последнем обработанном блоке или времени.
 */
export type GetMissedInfoParameters = unknown[]

/**
 * Запрос getmissedinfo API
 *
 * Используется для получения информации о пропущенных блоках и контенте.
 * Не требует авторизации.
 */
export interface GetMissedInfoRequest extends BaseRpcRequest<GetMissedInfoParameters> {
  /** Название метода */
  method: 'getmissedinfo'
  /** Параметры запроса - требуют уточнения */
  parameters: GetMissedInfoParameters
}
