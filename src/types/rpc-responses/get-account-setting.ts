/**
 * Типы для ответа RPC метода getaccountsetting
 *
 * # getaccountsetting - получение настроек аккаунта
 *
 * ## Когда использовать getaccountsetting:
 *
 * ✅ **Используйте для:**
 * - Получения настроек текущего авторизованного пользователя
 * - Проверки конфигурации аккаунта
 * - Восстановления пользовательских настроек
 *
 * **Пример использования:**
 * ```typescript
 * // Получить настройки аккаунта (требует авторизации)
 * const settings = await getByPRCWithAuth({
 *   method: 'getaccountsetting',
 *   parameters: [
 *     // Параметры зависят от реализации
 *   ],
 *   options: { auth: true } // Требует авторизации
 * })
 *
 * // Обработать настройки
 * if (settings.data) {
 *   // Настройки могут быть в виде строки JSON или объекта
 *   const parsedSettings = typeof settings.data === 'string'
 *     ? JSON.parse(settings.data)
 *     : settings.data
 * }
 * ```
 *
 * ## Структура данных:
 *
 * **getaccountsetting** возвращает строку или объект с настройками аккаунта.
 * В примере ответа `data` является пустой строкой, что может означать отсутствие настроек.
 *
 * ## Особенности:
 *
 * 1. **Формат данных**:
 *    - Может быть пустой строкой (если настроек нет)
 *    - Может быть JSON строкой (требует парсинга)
 *    - Может быть объектом (в зависимости от реализации)
 *
 * 2. **Авторизация**:
 *    - Требует авторизации для получения настроек текущего пользователя
 *
 * ## В старом приложении:
 *
 * - Вызывается через соответствующий RPC метод
 * - Используется для получения и восстановления настроек пользователя
 */

import type { BaseRpcResponse, StandardRpcTime } from './common'

/**
 * Полный ответ RPC метода getaccountsetting
 *
 * Используется для получения настроек аккаунта текущего пользователя.
 * Требует авторизации.
 *
 * В старом приложении:
 * - Используется для получения и восстановления настроек пользователя
 * - Настройки могут храниться в различных форматах
 */
export interface GetAccountSettingResponse extends BaseRpcResponse<string | Record<string, unknown>, StandardRpcTime> {}
